import { SrcDebounceDirective } from './src-debounce.directive';

describe('SrcDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new SrcDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
